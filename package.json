{
    "name": "docsmith",
    "version": "1.0.0",
    "private": true,
    "description": "Composer tooling for Docsmith documentation builds.",
    "scripts": {
        "docs:build": "php build-docs.php",
        "postinstall": "npx playwright install chromium"
    },
    "devDependencies": {
        "capturist": "^0.5.0",
        "playwright": "^1.62.1"
    }
}
